<h1>songの編集ページです</h1>

<%= form_for(@song,url: admin_product_disc_song_path(@song)) do |f| %>
	<h4>歌の名前</h4>
	<%= f.text_field :song_name %>
	<h4>歌の番号</h4>
	<%= f.text_area :song_number %>
	<%= f.submit '商品を編集する' %>
<% end %>