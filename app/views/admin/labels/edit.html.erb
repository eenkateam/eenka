<%= form_for(@label, url: admin_label_path) do |f| %>

	<p><%= f.label :label_name, "レーベル" %></p>
	<%= f.text_field :label_name %>

	<p><%= f.label :opinion, "紹介文" %></p>
	<%= f.text_area :opinion %>

	<%= f.submit "変更" %>

<% end %>

<%= link_to "レーベル詳細に戻る", admin_label_path(@label) %>