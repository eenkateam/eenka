<h1>これは商品一覧ページです</h1>

<table>
	<tr><th>商品名</th><th>値段</th><th>在庫枚数</th></tr>
	<% @products.each do |product| %>
		<tr>
	  		<th><%= link_to product.product_name, product_path(product), class: "show_#{product.id}" %></th>
	  		<th><%= product.price %></th>
	  		<th><%= product.stock %></th>
	  		<% if product.stock <= 0 %>
	  			<th>売り切れ</th>
	  		<% end %>
		</tr>
	<% end %>
</table>

